# svg2gcode

Как этим пользоваться :
- Если конфигурационный файл в этой же дирректори
python3 svg2gcode.py myfile.svg

Или:

python3 laser_raster.py myfile.svg --config my_custom_config.yaml --output my_board.nc

Для экспорта из KiCad в SVG обязательно ставить галку "Страница по размеру платы"

